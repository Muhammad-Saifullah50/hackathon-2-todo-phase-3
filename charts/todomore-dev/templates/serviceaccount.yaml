{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "todomore-dev.serviceAccountName" . }}
  labels:
    {{- include "todomore-dev.labels" . | nindent 4 }}
{{- end }}
